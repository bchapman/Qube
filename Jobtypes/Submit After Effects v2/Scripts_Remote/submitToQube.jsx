// ------------------------------------------------------------------------------------------------------// Create a qube data file//// Author: Brennan Chapmna// Version: 1.1//// Creates a qube data file to populate the qube submission interface.// This is saved in a file to speed up repeat submissions.// This file is called when submitting from the Qube GUI Interface.//// ------------------------------------------------------------------------------------------------------// ------------------------------------------------------------------------------------------------------// Load the Qube Tools if they aren't already loaded// ------------------------------------------------------------------------------------------------------var scriptFile = new File($.fileName);var qubeToolsFile = new File(scriptFile.parent.fsName + "/QubeTools.jsx");var Qube;if (!Qube) {    if (qubeToolsFile.exists) {        $.evalFile(qubeToolsFile);        writeLn("Qube Tools Loaded");    } else {        writeLn("Error: Qube Tools File not found.");    }} else {    writeLn("Qube Tools already loaded.");}// ------------------------------------------------------------------------------------------------------Qube.submitToQube();