// ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------// Render Quality// Author: Brennan Chapman// Version: 1.0//// Sets the render quality of the project at render time.// This only applies to expressions that link to a specified slider in the _Global_Controls comp.// This is useful for allowing artists to animate easily, and ensure that the render is at the right quality.//// ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------function setRenderQuality(quality) {    try {        var proj = app.project;        // Get the control comp        var controlComp;        for (var i=1; i<=proj.items.length; i++) {            if (app.project.items[i].name == "_Global_Controls") {                $.writeln("Found comp");                controlComp = proj.items[i];            }        }        // Get the Render Quality Layer        var qualityLayer;        for (var l=1; l<=controlComp.layers.length; l++) {            if (controlComp.layers[l].name == "RenderQuality") {                $.writeln("Found layer");                qualityLayer = controlComp.layers[l];            }        }        //Set the quality        qualityLayer.property("Effects").property("RenderQuality").property("Slider").setValue(quality);     } catch(error) {        this.log_file.writeln("WARNING: Unable to set render quality.");    }}